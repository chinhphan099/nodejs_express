extends ../layouts/template
block main
  h1 View user
  p #[b ID:] #[span #{user.id}]
  p #[b Name:] #[span #{user.name}]
  p #[b Phone:] #[span #{user.phone}]
  a(href="/users/remove/" + user.id) Remove #[br]
  a(href="/users") < User list
